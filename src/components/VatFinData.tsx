import React from "react";
import "./styles.css";

interface Props {
  //LeaseData: 
}

const VatFinData: React.FC<Props> = ({}) => {

  return (
    <div className="leaseForm">
        VATfinancing data form
    </div>
  );
};

export default VatFinData;
