import React from "react";
import "./styles.css";

interface Props {
   //LeaseData: 
}

const RentalData: React.FC<Props> = ({}) => {

  return (
    <div className="leaseForm">
        Rental data form
    </div>
  );
};

export default RentalData;
